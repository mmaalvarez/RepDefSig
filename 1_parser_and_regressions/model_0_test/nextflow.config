executor {
    name = 'slurm'
    queueSize = 1000
    submitRateLimit = '1 sec'
}

process {
    errorStrategy = {task.exitStatus == 143 ? 'retry' : 'ignore'}
    maxRetries = 4
}

trace {
    enabled = true
    file = "$PWD/trace/trace.txt"
}
